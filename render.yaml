services:
  - type: web
    name: tienlen-game
    env: node
    buildCommand: cd client && npm install && npm run build
    startCommand: cd server && npm install && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: / 