{
  "name": "tienlen-game",
  "version": "1.0.0",
  "description": "Tien Len card game with AI bots",
  "scripts": {
    "build": "cd client && npm install --production=false && npx vite build",
    "start": "cd server && npm install && npm start",
    "dev": "concurrently \"cd server && npm start\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
